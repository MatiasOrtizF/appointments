import { StyleSheet } from "react-native"

export const bookingsStyles = StyleSheet.create({
  container: {
    padding: 16
  },

  title: {
    fontSize: 18,
    fontWeight: "600"
  },

  cardSpacing: {
    marginBottom: 12
  }
})